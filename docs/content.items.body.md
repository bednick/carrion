# Контент: оборона (слот `body`)

Источник правды по слоту `body`: домен, ось, архетипы, характеристики предмета и список предметов слота. Общие
правила системы предметов (R1–R9) и функциональность слотов — [`content.items.md`](content.items.md); боевая
идентичность фракций — [`factions.md`](factions.md); событийная модель — [`combat-events.md`](combat-events.md).

## Домен слота

**Снижение получаемого урона — и только оно.** Раньше домен слота включал восстановление (лечение за удар/
убийство) как второй архетип наравне с бронёй — это решение пересмотрено: **весь хил переехал в `amulet`**
(см. открытый вопрос по `amulet` в `content.items.md`), `body` больше не про выживание в широком смысле, а строго
про **митигацию**: что происходит с уроном между «враг ударил» и «HP героя уменьшилось».

Это ровно строка «безусловная митигация» из матрицы противодействия фракций
([`factions.md`](factions.md)) — вспомогательный инструмент против **всех трёх** фракций, не чемпион ни одной.
Слот сознательно остаётся вне крест-накрест лут-цикла R10 (как и щиты `hand_left`): это нейтральная страховка,
не анти-фракционный инструмент.

## Ось слота

Архетипы — открытый веер вдоль одной оси **«форма снижения урона»**: от пассивной константы (броня как
процент, мультипликативно снижает удар; на слабом ударе может свести его в ноль — см. `docs/mechanics.md`
§«Броня vs щит») до реактивных/условных форм (шипы, порог по HP), с одним намеренным
исключением-трейдоффом на верхушке семейства (R7: топ-предмет слота платит явную цену). Как и у `hand_right`, это
не жёсткая бинарная пара полюсов (как `hand_left`), а расширяемый список — новые формы митигации можно добавлять
без переписывания оси.

| Предмет                               | Профильный стат                                                    | Природа                                 |
|---------------------------------------|--------------------------------------------------------------------|-----------------------------------------|
| Блестящие латы (`gleaming_plate`)     | `damage_reduction`                                                 | пассивная константа, «чистая» броня     |
| Шипастый нагрудник (`spiked_cuirass`) | `damage_reduction` (ниже) + rider `thorns_flat`                    | пассивная + безусловный побочный эффект |
| Латы отчаяния (`desperate_plate`)     | `damage_reduction`, включается при HP < порога                     | реактивная по состоянию героя           |
| Тяжёлые латы (`heavy_plate`)          | `damage_reduction` (максимум в семействе) + rider замедления атаки | пассивная константа + осознанная цена   |

## Отклонённые направления

- **Лечение/лайфстил в `body`** — старый план, отменён этой правкой. Восстановление целиком переезжает в
  `amulet`; `body` не восстанавливает HP ни при каких условиях.
- **Вероятностный блок / контрудар (`block_chance`/`counter_chance`)** — это ось `hand_left` (R1: слот = одна
  ось), задваивать домен целиком нельзя. Единственное сознательное исключение — `thorns_flat` как **rider**
  (не как отдельный полноценный узел): он уже безусловно (без броска) существует на `spiked_shield` в
  `hand_left`, и параллельное использование того же тега на `body` не создаёт новый домен, а просто даёт второй
  предмет с той же безусловной механикой — жанровый прецедент есть даже у прямого ориентира: в Overlooting один
  и тот же «шипастый» вариант существует одновременно и на нагруднике, и на щите одного сета.
- **Уклонение** — принадлежит Нежити-врагу и не выдаётся герою ни в одном слоте (см. `content.items.hand_right.md`,
  `content.items.hand_left.md`).
- **Гарантированный блок в начале боя / перезаряжаемый заслон** (любой пул поглощения — что per-fight, что на
  весь забег с восстановлением между боями) — рассмотрено и отклонено: не нужно прямо сейчас. Кросс-файтовый
  вариант дополнительно требует полноценной «Системы стаков» ([`roadmap.md`](roadmap.md), раздел «Система
  стаков (заряды/накопление)») — тяжеловесная инфраструктура, которую нет смысла заводить ради одного предмета.
- **Резист к статусам** (сокращение длительности проклятия/кровотечения) — отклонено пока: сами статусы с
  длительностью не реализованы в движке ([`roadmap.md`](roadmap.md)), резистить нечего. Возможное направление
  на будущее, после статус-системы.

## Характеристики предмета

Combat-статы живут в `behavior.ts` (R9), `stats()` — единый источник правды для тултипа и информатора (R4). Ни один
из пяти предметов не требует нового событийного типа или нового движкового примитива — четыре реализуются на
существующих хуках без единой строчки нового движкового кода, один (`heavy_plate`) использует уже существующий
кросс-slot примитив `weaponTimerMod`.

- **Блестящие латы** (экс-`robe_scrap`) — только `damage_reduction` (`standardArmor`, уже в коде). Верхняя
  планка «чистой» брони семейства — один из полюсов оси. Переименование: смена `id`/`name`/иконки, синхронно
  везде, где использовался `robe_scrap` — сама механика не меняется.
- **Шипастый нагрудник** — вклад в канал `armor_pct` (тир `more`, ниже, чем у «чистой» брони) + безусловный
  вклад в канал `thorns_flat` — фикс. число урона назад за каждый удар, независимо от его силы — как и у
  `spiked_shield`. Шипы — единая engine-owned стадия (`CombatEngine.runReactiveStage`,
  [`combat-events.md`](combat-events.md) §5), срабатывает независимо от исхода блока и от того, каким
  слотом надет щит: специального второго хука «поймать чужой блок» больше не нужно, стадия одна на весь бой.
- **Латы отчаяния** — `damage_reduction`, но с условием: применяется полная величина, только когда `ctx.view.
  heroHp / ctx.view.heroMaxHp` ниже фиксированного порога (кандидат: 30%, не скейлится редкостью — ровно один
  профильный стат на архетип, R5). `HandlerContext.view` (`CombatView.heroHp`/`heroMaxHp`) уже читаются в
  движке — новый тег `last_stand` (декларативный), новых хуков не нужно.
- **Тяжёлые латы** — `damage_reduction` (максимум во всём семействе) как основной хук, плюс кросс-slot
  `weaponTimerMod` без `scope` (бьёт по каждому надетому оружию, не привязан к `hand_right`) с
  `intervalMult > 1` (замедляет атаку) как rider. Технически уже полностью поддержано
  существующим примитивом (`src/items/behavior.ts`, тот же, что использует `light_gloves`/`comfortable_gloves`
  — см. [`content.items.hand_left.md`](content.items.hand_left.md)), только со знаком наоборот: там
  `intervalMult < 1` (быстрее), здесь `> 1` (медленнее). Замыкает старое правило R7 («осознанный минус на
  топ-предмете `body`») — единственный явно ограниченный предмет слота: чем выше редкость, тем сильнее и
  защита, и штраф. Тег `slow` уже существует в реестре (используется архетипом `heavy` у `hand_right`) и
  семантически подходит без изменений.

**Лестница апгрейда** — как везде: `common → uncommon → rare → epic` (4 ступени), скейл капается на `epic`;
`legendary` — авторская редкость, скейлом недостижима.

## Предметы

| Предмет            | id                | Профильный стат               | tags               | База (common)                | По редкости (common/uncommon/rare/epic/legendary)        | Статус                                          |
|--------------------|-------------------|-------------------------------|--------------------|-------------------------------|-----------------------------------------------------------|-------------------------------------------------|
| Блестящие латы     | `gleaming_plate`  | `damage_reduction`            | `armor`            | −10% урона/удар                | 10 / 15 / 23 / 34 / 51%                                    | есть, переименование `robe_scrap` завершено     |
| Шипастый нагрудник | `spiked_cuirass`  | `damage_reduction` + rider    | `armor,thorns`     | −7% урона/удар, шипы 1        | reduction 7/11/15/20/24%; thorns 1/2/3/4/5 (флэт, за удар)  | есть                                            |
| Латы отчаяния      | `desperate_plate` | `damage_reduction` (условный) | `armor,last_stand` | −18% урона/удар при HP < 30%   | 18 / 27 / 37 / 48 / 60% (порог фиксирован, 30%)            | есть                                            |
| Тяжёлые латы       | `heavy_plate`     | `damage_reduction` + rider    | `armor,slow`       | −15% урона/удар, +8% скорость атаки (всё оружие) | reduction 15/24/34/46/60%; interval +8%/+11%/+15%/+20%/+20% | есть                                            |

Модель снижения — мультипликативная (доля 0..1 на удар, стакается перемножением остатка; см.
`docs/mechanics.md` §«Броня vs щит», `src/combat/mitigation.ts`). До 2026-07 была флэт-формулой (вычитание
фиксированного числа) — переведена на проценты, потому что флэт-вычитание сводило слабый урон мобов в ноль.
Итог считается дробным и округляется **стохастически** один раз, в `applyDamage`: пола в 1 урон нет, на
слабом ударе высокая броня может свести его в ноль (UI показывает «Блок»). Все числа — placeholder, **не проверены Monte-Carlo** на финальную калибровку (прогонялись
только smoke-тестом `npm run balance` на трёх стартовых зонах после перевода на %, чтобы исключить регрессии
вин-рейта, не для точной подгонки).

## Порядок реализации

Ни один пункт не требует новой движковой инфраструктуры — только новые предметы поверх существующих примитивов:

1. **Шипастый нагрудник** — прямое повторение уже реализованного `spiked_shield`, другой слот.
2. **Тяжёлые латы** — `standardArmor` + уже существующий `weaponTimerMod` (используется `light_gloves`), просто
   `intervalMult > 1`.
3. **Латы отчаяния** — обычный `on damage` хук, условие на `ctx.view.heroHp/heroMaxHp` (уже доступны).
4. **Блестящие латы** — переименование `robe_scrap` (id/name/иконка) + пересмотр чисел (механика не меняется).

## Реализовано в коде (2026-07)

Все четыре предмета заведены в `src/items/*` и зарегистрированы (`registry.ts`, `icons.ts`). Сборка (`npm run
build`, `tsc --noEmit`) чистая.

- `gleaming_plate` — переименование `robe_scrap` доведено до конца: новый `id`/`name`/иконка (полированные латы
  вместо лоскута мантии), `registry.ts`/`icons.ts`, пример JSON в `content.md`. Число пересмотрено:
  `standardArmor({ reduction: 2 })` (было `1`), даёт ряд `2/3/5/7` на дефолтном скейле `1.5` — совпадает с
  целевыми числами без флэт-таблицы.
- `spiked_cuirass` — `on.damage` хук совмещает снижение (флэт-таблица `1/2/2/3`) и rider `thorns_flat`
  (`1/2/3/4/5`, фикс. число за удар, независимо от его силы; см. «Шипы: независимость от блока» ниже) —
  до 2026-08-08 это была доля `thorns_ratio` от урона удара до снижения (до 2026-08-06 доля ошибочно
  считалась от уже сниженного значения), переведена на флэт-константы.
- `desperate_plate` — обычный `standardArmor`-паттерн (`on.damage` → `replace`), но хук выходит рано, если
  `ctx.view.heroHp / ctx.view.heroMaxHp >= 0.3`. Флэт-таблица `3/5/7/10`, порог `30%` не скейлится редкостью.
- `heavy_plate` — флэт-таблица снижения `3/5/7/10` + `weaponTimerMod` с `intervalMult = 1 +
  INTERVAL_PENALTY` (`8/11/15/20%`) — тот же примитив, что `light_gloves`, со знаком наоборот. До
  2026-08-07 контрибуция была заскоуплена на `hand_right` (`scope: { targetSlot: 'hand_right' }`); scope
  снят, чтобы «Скорость атаки» как эффект била по любому надетому оружию, а не только по главному —
  функционально не изменилось (`hand_right` пока единственный слот с `weapon()`), но снимает будущую
  нестыковку, если появится второй weapon-слот.

Слот `body` вне крест-накрест цикла R10 (не чемпион ни одной фракции). Конкретная зона-источник лута для каждого
предмета этим доком не отслеживается — единственный источник правды `src/zones/*/config.json` (R11,
`content.items.md`).

## Переход на %-модель (2026-07)

Флэт-вычитание сводило слабый урон мобов в ноль (см. `docs/roadmap.balance.md` §«Эмпирические уроки») — все
четыре предмета переведены на мультипликативное %-снижение (`src/combat/mitigation.ts`, см. `docs/mechanics.md`
§«Броня vs щит»). Механика та же (один `on damage` хук на предмет, райдеры — шипы/замедление/HP-порог — не
менялись), поменялась только формула и единицы: `gleaming_plate` через `standardArmor({ pct })`, три остальных —
через собственные таблицы `Record<Rarity, number>` теперь в долях 0..1 вместо флэт-очков. Ветка «броня свела
удар ровно в ноль → событие `block`» убрана из всех четырёх. ~~Симметрично переведена и броня мобов
(`defense.armor`, `CombatEngine.enemyDefend`).~~ — **отменено 2026-07-31:** броня мобов возвращена на числовой
плоский вычет (`mitigateFlat`), процентная модель осталась **только** у героя; модели теперь разные намеренно,
см. `docs/mechanics.md` §«Броня vs щит» и `docs/content.mobs.format.md` §«Защита». Броня героя (эти четыре
предмета) с тех пор не менялась. Итоговые числа
(таблица выше) — placeholder, требуют полноценного прогона `npm run balance` для точной калибровки (сделан
только smoke-тест на регрессию вин-рейта).

## Стохастическое округление (2026-07, шаг 2)

Первая %-модель округляла результат каждого источника через `Math.round` с полом в 1 урон — из-за этого
проценты не работали на мелких ударах вообще (броня 10% против удара в 2 давала `round(1.8) = 2`), а высокая
броня упиралась в тот же пол, что и средняя. Теперь:

- `mitigateDamage` **не округляет** — урон едет по цепочке хуков дробным, стак брони точен;
- единственное округление — стохастическое, в `CombatEngine.applyDamage` (`stochasticRound`): дробный остаток
  = вероятность округлить вверх, матожидание урона ровно `урон × (1-p)` на любом размере удара;
- **пол в 1 урон снят** — броня может свести удар в ноль. Такой удар движок отдаёт как событие `block`
  (автор — движок, не щит), UI показывает флоатер «Блок». Ветка `block` из предметов, убранная на первом
  шаге, вернулась на уровне движка — и работает симметрично на герое и на мобах.

Шипы `spiked_cuirass` тоже перестали округлять свою долю — `damage`-спавн округлится сам в `applyDamage`
(раньше 20% от удара в 1 всегда давали 0).

## Тихий rider-урон (2026-07, шаг 3)

Побочка шага 2: терминал `block` за нулевое округление выдавался и на **вторичный** урон. Шипы 10% от удара
в 3 дают `0.3` — ноль в 70% случаев, то есть над мобом почти на каждом попадании всплывал флоатер «Блок» со
звуком, будто моб что-то заблокировал. Математика шипов при этом была верной, но читалась как поломка.

- `applyDamage` получил параметр `rider` (`CombatEngine.apply`, `case 'damage'`: `e.thorns || e.splash`):
  вторичный урон, округлившийся в 0, гаснет молча, без события `block`. HP-логика не изменилась.
- Флоатер «Блок» остался за основным ударом — там ноль действительно значит «броня погасила удар».
- Заодно на дробь переведён сплеш `broadaxe` (`Math.round(dmg * ratio)` съедал нижнюю часть шкалы:
  20% от урона в 2 = `round(0.4)` = 0, и цель молча выпадала из списка задетых).

Шипы мобов (`defense.thorns`) остаются флэт-числом. **Обновление (2026-08):** шипы героя (`spiked_cuirass`,
`spiked_shield`) переведены с доли `thorns_ratio` на тот же флэт-канал `thorns_flat`, что и у мобов — модели
теперь симметричны, направление миграции оказалось обратным изначально предполагавшемуся.

## Шипы: независимость от блока

Инвариант, который должны держать оба шипастых предмета героя (`spiked_cuirass`, `spiked_shield`): урон
шипов нельзя заблокировать щитом — срабатывает независимо от исхода блока. Изначально (пайплайн по порядку
слотов) это давалось ценой второго `on.block`-хука у `spiked_cuirass`, ловившего блок чужого слота отдельным
проходом (история фикса не переписана, см. git).

С переходом на channel/trigger-модель ([`combat-events.md`](combat-events.md)) это свойство самой
архитектуры, не точечный костыль: `thorns_flat` — один канал, читаемый **одной** engine-owned реактивной
стадией после того, как исход block/hit уже решён, независимо от него — фикс. число, не зависящее от силы
удара. Порядок слотов на это не влияет в принципе — второй хук не нужен ни одному будущему шипастому предмету.

## Открытые вопросы

1. ~~Числа `gleaming_plate`~~ — **решено (2026-07), модель заменена.** См. «Переход на %-модель» — теперь
   `standardArmor({ pct: REDUCTION })` с явной таблицей `Record<Rarity, number>` в `behavior.ts`,
   ряд `10/15/23/34/51%` (раньше — скейл `1.5` от `0.1`, который давал дробные `33.75/50.625%` под
   округлённым тултипом).
2. **Порог `desperate_plate` (30%) — не финализирован**, взят как круглое число «конец боя». Стоит проверить
   Monte-Carlo, не оказывается ли эффект либо бесполезным (герой обычно не проседает так низко), либо наоборот
   слишком сильным окном для конкретных билдов.
3. ~~Дом новых предметов в лут-таблицах~~ — **снято (2026-07, R11).** Конкретная раскладка лута по зонам больше
   не отслеживается в этом доке — единственный источник правды `src/zones/*/config.json`.
